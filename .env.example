APP_NAME=GTD TODOs
APP_ENV=development
APP_HOST=0.0.0.0
APP_PORT=8080
DATABASE_URL=sqlite:////data/todo.db

# Auth — generate a secret: python -c "import secrets; print(secrets.token_hex(32))"
AUTH_SECRET_KEY=replace-me-with-a-real-secret
WEBAUTHN_RP_ID=localhost
WEBAUTHN_ORIGIN=http://localhost:8081